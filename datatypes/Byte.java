class Byte
      {
            public static void main(String[] args)
                   {
                         //byte is 8 bit signed integer   range(-128 to 127) byte can store the valuse if it is in range (-127 to 128)
                         byte range=127;
                         System.out.println("range:" + range);

                   }
       }